query keyboardPosts($shortId: String!) {
  keyboard(shortId: $shortId) {
    id
    shortId
    images600
    name
    posts {
      id
      body
      created
      user {
        username
      }
    }
  }
}
