query KeyboardPosts($shortId: String!, $limit: Float!) {
  keyboardPosts(shortId: $shortId, limit: $limit) {
    id
    shortId
    images600
    name
    posts {
      id
      body
      created
      user {
        username
      }
    }
  }
}
