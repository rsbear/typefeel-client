query KeyboardData($shortId: String!) {
  keyboard(shortId: $shortId) {
    closed
    connector
    editions {
      id
      name
      price
      suggestedPrice
      cases
      plates
    }
    groupBuy
    groupBuySoon
    id
    images1500
    interestCheck
    joins {
      id
      caseChoice
      layoutChoice
      plateChoice
    }
    name
    shortId
    size
  }
}
